import numpy as np

# start stop step
num = np.arange(10,dtype = "float")
print(num)

arr = np.arange(1,10,2,dtype = "int")
print(arr)

# number ติดลบ # ถ้าอยากได้ค่าตัวเริ่มต้นต้องบวกเพิ่มอีก 1 เช่น -5,6 ก็ค่าคือ จะเริ่มที่ 0
num1 = np.arange(-5,6,dtype = "complex")
print(num1)